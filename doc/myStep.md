1. 不要安裝檔 直接是執行檔
2. ui 你寫失敗 沒有縮放 然後不能滾動
3. ui 寫成 什麼組合鍵 -> 組合鍵 ->組合鍵 -> 組合鍵-> 組合鍵 -> 為了什麼 (沒的留空)
4. 你現在的主題我很喜歡 只是佈局不對 然後視窗縮放 滾動有問題
5. 一開始尺寸 就要對的 然後 改成不能縮放 好了
再加上 箭頭 不要打 - > 用真的圖案 然後 讓UI 再好看一點
6. 按鍵都改成一致藍框那個 視窗再框一點 我不想要看到 () 文字道下一行
7. 真的是大寫的才打大寫 不然都顯示小寫
8. 讀取視窗 px 然後 既然有滾動 高度就不用那麼高